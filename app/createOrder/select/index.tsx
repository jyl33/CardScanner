import { View, ActivityIndicator, Alert } from 'react-native';
import CardTableWithSelect from '@/components/CardTableWithSelect';

const CardSelectionScreen = () => {
  
  return (
    <View>
      <CardTableWithSelect/>
    </View>
  );
};

export default CardSelectionScreen; 